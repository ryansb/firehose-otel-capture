[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
aws-cdk-lib = "==2.0.0-rc.21"
constructs = ">=10.0.0,<11.0.0"

[dev-packages]
ptpython = "*"

[scripts]
synth = "cdk synth --no-version-reporting"
diff = "cdk diff --no-version-reporting"
deploy = "cdk deploy --no-version-reporting"

[requires]
python_version = "3.9"

[pipenv]
allow_prereleases = true